void OnBuzzer() {
  // הוצאת צליל בתדר 1 קילו הרץ ברגל 3 //

  // הוצאת צליל בתדר 1 קילו הרץ ברגל 3 //
  tone(buzz_pin, 10000);
  tone(buzz_pin, 2000);
}  // Delay to keep door open



// הוצאת צליל בתדר 1 קילו הרץ ברגל 3 //
void OffBuzzer() {  // הוצאת צליל בתדר 1 קילו הרץ ברגל 3 //
  tone(buzz_pin, 0);
  tone(buzz_pin, 0);
}  // Adjust angle for closing
